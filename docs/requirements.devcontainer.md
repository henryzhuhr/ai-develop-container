# Dev Container 预置 VS Code Remote 需求文档

## 1. 背景

当前目标是让用户使用本地 VS Code 通过 Dev Containers / Remote Development 进入该容器时，尽量减少首次连接时的准备时间，并尽可能避免或缩短 VS Code Server 的在线下载过程。

需要明确的一点：

- 本场景使用的是微软的 `VS Code Server`，不是 Coder 的 `code-server`。
- `VS Code Server` 通常与用户本地 VS Code 客户端版本或 commit 绑定。
- 因此，“完全不下载”只有在客户端版本被严格控制时才有较高可行性；如果客户端版本不固定，仍可能发生重新下载。

本需求文档用于补齐约束、目标和验收标准，作为后续实现依据。

## 2. 目标

请补充你希望达到的目标，建议至少回答以下问题：

- 目标用户是谁：
  - 例如：公司内部开发者、外部客户、CI 环境使用者。
- 使用方式是什么：
  - 例如：本地 VS Code + Dev Containers 扩展连接 Docker 容器。
- 你最关心的指标是什么：
  - 例如：首次进入容器时间、离线可用性、镜像体积、跨平台兼容性、维护成本。
- 你希望“无需再次下载”达到什么程度：
  - 例如：完全离线可进入。
  - 或：大多数情况下不下载，只有 VS Code 版本变化时才下载。

## 3. 非目标

请确认以下哪些不是本次要解决的范围：

- 不提供浏览器访问的 Web IDE。
- 不解决 VS Code Desktop 客户端本身的安装与分发。
- 不保证任意版本的本地 VS Code 都无需下载远端 server。
- 不处理所有第三方扩展的离线分发。

如有例外，请在下面补充：

```text
待补充
```

## 4. 已知技术约束

这些约束会直接影响实现方案，请确认是否接受：

### 4.1 VS Code Server 与客户端版本绑定

- Remote Development 连接远端时，会使用与本地 VS Code 匹配的 server 版本。
- 如果用户本地 VS Code 版本升级，对应 server 版本也可能变化。
- 如果镜像中未预置对应版本，首次连接仍会触发下载。

### 4.2 “预置 server”有前提

常见可行路径通常有两类：

- 路径 A：锁定组织内统一的 VS Code 版本，然后在镜像中预置对应 commit 的 `~/.vscode-server/bin/<commit>`。
- 路径 B：接受首次连接时仍可能下载，但把其他依赖和常用扩展都预装，尽量缩短初始化时间。

### 4.3 许可、分发与来源限制

- 若要在镜像构建时预置 VS Code Server，需要明确下载来源、版本管理方式和许可风险。
- 如果你的使用环境需要内网、离线或镜像代理，需要一并定义制品来源与缓存策略。

## 5. 候选实现方案

请在下面选择或补充你更倾向的方案。

### 推荐默认决策

在你没有提供更多约束前，我建议先按下面的决策推进第一版：

- 默认选择方案 A。
- 本地 VS Code 默认限定为 `Stable` 渠道。
- 默认锁定 1 个明确的 VS Code 版本与 commit。
- 默认优先支持 `amd64`，如有明确需要再补 `arm64` 或多架构。
- 默认覆盖容器内 `root` 用户。
- 默认允许镜像构建阶段访问外网下载 VS Code Server 制品。
- 默认不处理浏览器版 IDE。
- 默认不处理第三方扩展的完整离线分发，只保留后续扩展点。
- 默认接受以下行为：
  - 当本地 VS Code 版本与镜像预置版本一致时，不再下载 server。
  - 当本地 VS Code 版本变化时，允许 Remote Development 按需重新下载对应 server。

如果你同意这组默认值，我会按这条路径直接实现第一版。

### 方案 A：锁定本地 VS Code 版本并预置对应 server

说明：

- 在镜像构建时下载指定 commit 的 `VS Code Server`。
- 放入约定目录，例如 root 用户下的 `~/.vscode-server/bin/<commit>`。
- 适合内部统一开发环境。

优点：

- 首次连接最快。
- 更容易支持离线或弱网环境。

代价：

- 需要维护 VS Code 版本与 commit 的映射。
- 用户本地 VS Code 版本不一致时，仍可能重新下载。

请补充：

```text
是否选择：建议默认选“是”
目标 VS Code 版本：待你指定；若暂未指定，实现时可先预留构建参数
目标 commit（如果已知）：待你指定；若未知，可通过版本映射或显式传参处理
是否允许锁定本地 VS Code 版本：建议默认“是”
```

### 方案 B：不预置 server，只优化首次初始化体验

说明：

- 不尝试预埋 `VS Code Server`。
- 优化镜像内容，例如常用工具链、shell、git、语言依赖、扩展缓存、工作目录结构。

优点：

- 维护成本更低。
- 对客户端版本变化更稳健。

代价：

- 首次连接仍可能下载 server。

请补充：

```text
是否选择：建议默认“不选”
你希望预装哪些工具或扩展：如后续需要，可作为第二阶段补充
```

### 方案 C：同时支持在线与离线模式

说明：

- 在线环境默认按需下载。
- 离线环境通过构建参数、挂载缓存或制品仓库注入指定 server。

优点：

- 灵活。

代价：

- 实现复杂度和文档成本更高。

请补充：

```text
是否选择：建议默认“不选”
在线环境约束：默认公网可访问官方制品
离线环境约束：第一阶段暂不覆盖
制品存放位置：如未来需要，建议使用内部制品仓库或对象存储
```

## 6. 你需要补充的关键信息

请尽量完整填写下面内容：

### 6.1 用户环境

```text
用户操作系统（macOS / Windows / Linux）：待确认；若无特殊要求，默认 macOS / Windows 混合桌面环境
本地 VS Code 渠道（Stable / Insiders）：建议默认 Stable
是否统一版本：建议默认是
是否允许自动升级 VS Code：建议默认否，至少在第一阶段先锁定版本
网络环境（公网 / 代理 / 内网 / 完全离线）：建议默认公网或可访问官方制品源
```

### 6.2 容器运行环境

```text
目标架构（amd64 / arm64 / 多架构）：建议默认 amd64
运行方式（docker run / docker compose / Dev Container CLI / Codespaces 类似流程）：建议默认 Docker + VS Code Dev Containers
容器内默认用户（root / 非 root）：建议默认 root
工作目录：建议默认沿用当前仓库约定
是否要求开箱即用的 git / ssh / zsh / sudo：建议默认 git / ssh 为必需，zsh / sudo 视需要补充
```

### 6.3 预装内容

```text
必须预装的语言与版本：默认沿用当前镜像中的 Node.js、Go、Python/uv
必须预装的 CLI：默认沿用当前镜像中的 codex、claude-code、cs、git、ssh
必须预装的 VS Code 扩展：第一阶段建议不内置，除非你给出明确列表
是否要预置扩展缓存：第一阶段建议否
是否要预置 dotfiles / shell 配置：第一阶段建议否
```

### 6.4 安全与合规

```text
是否允许在镜像构建时访问外网：建议默认是
是否必须走内部制品源或镜像源：建议默认否
是否允许把 VS Code Server 制品内置到公开镜像：待你确认
是否有许可证或供应链审计要求：待你确认
```

## 7. 验收标准

请定义你接受的验收标准。建议至少包含以下维度：

```text
首次连接耗时上限：建议默认首连额外准备时间不超过 30 秒
在何种条件下禁止下载远端 server：建议默认当本地 VS Code 版本已被组织锁定且镜像已预置匹配 commit 时
在何种条件下允许自动下载：建议默认当用户本地 VS Code 版本与镜像预置版本不一致时
镜像体积上限：待你确认；若无要求，可先以功能正确为主
是否要求支持离线启动：第一阶段建议否
是否要求支持 amd64 和 arm64：第一阶段建议否，默认先支持 amd64
```

## 8. 建议的实现边界

如果你没有特别要求，我建议按下面的边界实现第一版：

- 第一阶段只支持 `Stable` 渠道。
- 第一阶段只支持 1 个明确的 VS Code 版本或 commit。
- 第一阶段优先支持当前主力架构。
- 第一阶段先覆盖 root 用户场景。
- 第一阶段把需求拆成两部分：
  - 镜像预置逻辑。
  - 使用说明与验证脚本。

## 9. 建议的交付物

如果你确认需求，我预计会交付这些内容：

- Dockerfile 改动。
- 构建脚本改动，用于传入 VS Code 版本、commit 或制品地址。
- 可选的校验脚本，用于确认 server 是否已预置到正确目录。
- README 或 `docs/` 文档，说明使用方式与限制。
- 冒烟验证命令。

## 10. 你的补充结论

请直接在这里总结最终决策：

```text
选定方案：建议默认方案 A
必须满足的目标：当本地 VS Code 版本与镜像预置版本一致时，进入容器开发不再下载 VS Code Server
可以接受的妥协：当本地 VS Code 版本升级或不一致时，允许按需下载对应 server
暂不处理的范围：浏览器版 IDE、完整离线扩展分发、多渠道 VS Code、多架构一次性全覆盖
```
